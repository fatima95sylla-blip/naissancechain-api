APP_NAME="NaissanceChain API"
APP_ENV=production
APP_KEY=base64:YOUR_APP_KEY_HERE
APP_DEBUG=false
APP_URL=http://naissancechain.local
APP_TIMEZONE=Africa/Abidjan

APP_LOCALE=fr
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=fr_FR

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=naissancechain_prod
DB_USERNAME=root
DB_PASSWORD=

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database
CACHE_STORE=file
CACHE_PREFIX=naissancechain_

SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

SANCTUM_STATEFUL_DOMAINS=http://naissancechain.local
SANCTUM_STATELESS_DOMAINS=http://naissancechain.local

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://flutter.local
CORS_ALLOWED_METHODS=GET,POST,PUT,PATCH,DELETE,OPTIONS
CORS_ALLOWED_HEADERS=Content-Type,Authorization,X-Requested-With,X-CSRF-TOKEN
CORS_ALLOW_CREDENTIALS=true

# Upload Configuration
UPLOAD_MAX_SIZE=10240
UPLOAD_ALLOWED_MIME_TYPES=pdf,jpg,jpeg,png
UPLOAD_PATH=uploads
STORAGE_PATH=storage/app/public

# Queue Configuration
QUEUE_FAILED_DRIVER=database
QUEUE_RETRY_AFTER=90
QUEUE_AFTER_COMMIT=true

# Cache Configuration
CACHE_TTL=3600
CACHE_TAG_PREFIX=naissancechain

# Security Configuration
SECURE_COOKIES=true
SECURE_HEADERS=true
FORCE_HTTPS=false

# Performance Configuration
OPCACHE_ENABLE=true
COMPRESSION_ENABLED=true
KEEP_ALIVE_TIMEOUT=5
